<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.springboot.dao.INoticeDao">

	
	<select id="admin_notice_list" resultType="com.study.springboot.dto.NoticeDto">
		select * from Notice
	</select>



<!-- 	<select id="noticeList" resultType="com.study.springboot.dto.NoticeDto">
		select * from Notice
	</select>
	
	<select id="noticeDetail" resultType="com.study.springboot.dto.NoticeDto">
		select * from Notice where notice_idx=#{param1}
	</select>
	
	<insert id="insertNotice" parameterType="com.study.springboot.dto.NoticeDto">
		insert into Notice
		values ( Notice_seq.nextval, #{notice_name}, #{notice_title}, 
		#{notice_content}, sysdate )
	</insert>
	
	<delete id="deleteNotice">
	  DELETE FROM Notice
	  WHERE notice_idx=#{ param1 }
	</delete> -->
	
</mapper>